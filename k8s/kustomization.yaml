apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

secretGenerator:
  - name: dns-deception
    literals:
      - DECEPTION_SECRET_KEY=CHANGEME
